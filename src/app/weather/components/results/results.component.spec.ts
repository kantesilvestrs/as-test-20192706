/**
 * Notes:
 *  - Due to time constraints I didn't start writing unit tests for this component, it should be much easier that the search component
 */
describe("Results Component", () => {
  it("should display correc data", () => {});
  it("should update with the correct data", () => {});
  it("should display nothing if no data supplied", () => {});
});
